# PostgreSQL connection string
DATABASE_URL=postgresql://neurotube:neurotube@localhost:5432/neurotube_dev

# Google Gemini API key (required)
GEMINI_API_KEY=your-key-here

# Server port (default: 3000)
PORT=3000

# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=debug

# Node environment: development | production (default: development)
NODE_ENV=development

# Static files directory for SPA serving (default: auto-detected relative to server)
# In Docker, client/dist/ is pre-built and co-located; no need to set this.
# STATIC_DIR=./client/dist

# JWT Authentication (required for auth)
# Secret key for signing JWT tokens (min 32 characters)
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
# Access token TTL (default: 15m)
JWT_ACCESS_EXPIRES_IN=15m
# Refresh token TTL (default: 7d)
JWT_REFRESH_EXPIRES_IN=7d

# PostHog analytics (optional — leave empty to disable)
# Typically you use the same project API key for both server and client.
# POSTHOG_API_KEY=phc_your_project_api_key
# POSTHOG_HOST=https://us.i.posthog.com
# VITE_POSTHOG_API_KEY=phc_your_project_api_key
# VITE_POSTHOG_HOST=https://us.i.posthog.com
